/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com',"upload.wikimedia.org"], // Add the hostname of the external image source
      },
};

export default nextConfig;
